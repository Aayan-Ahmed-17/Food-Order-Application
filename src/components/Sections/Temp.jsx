import React from 'react'
import useFetchData from '../../hooks/fetchData'

const Temp = () => {
    const res = useFetchData()
    console.log(res)
  return (
    <div>
      
    </div>
  )
}

export default Temp
